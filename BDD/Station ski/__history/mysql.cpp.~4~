//---------------------------------------------------------------------------

#pragma hdrstop

#include "mysql.h"
//---------------------------------------------------------------------------
#pragma package(smart_init)

 MySQL ::MySQL()
 {
	 SQL_G = mysql_init(NULL);
 }

MySQL :: connexion(string adresse, string id, string mdp, string table)
 {

	 if (!mysql_real_connect(SQL_G, "127.0.0.1", "root", "", "/*nomBDD*/", 0, NULL, 0))
	 {
   // la connection a échoué
	 }
	 else
	 {
   // la connection a réussi
	 }
 }


 MySQL :: deconnexion()
 {
	mysql_close(SQL_G);
 }


 MySQL :: getEtat()
 {

 }
