//---------------------------------------------------------------------------

#include <vcl.h>
#pragma hdrstop

#include "IHM.h"
//---------------------------------------------------------------------------
#pragma package(smart_init)
#pragma resource "*.dfm"
TForm1 *Form1;
//---------------------------------------------------------------------------
__fastcall TForm1::TForm1(TComponent* Owner)
	: TForm(Owner)
{
std::vector<std::string,sequence*>seq;
trameManager*trame=new trameManager();
char*dmxx;
int i;
dmxx = new char[512];

	for (i = 0; i < 8; i++)
	{
		dmxx[i]=230+i;
	}
trame->dmx=dmxx;

//sequences
seq1=new sequence(1,20,trame);
seq2=new sequence(1,20,trame);
seq3=new sequence(1,20,trame);




//scene
scn1=new scene(
}
//---------------------------------------------------------------------------
