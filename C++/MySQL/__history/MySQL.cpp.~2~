//---------------------------------------------------------------------------

#pragma hdrstop

#include "MySQL.h"
//---------------------------------------------------------------------------
#pragma package(smart_init)

bool MySQL::Connect(char*Addr,char*Id,char*Mdp,char*Table)
{

	if (!mysql_real_connect(SQL,Addr,Id,Mdp,Table, 0, NULL, 0))
	{
	   Etat=false;
	}
	else
	{
	   Etat=true;
	}

	return Etat;
}

bool MySQL::Disconnect()
{
	mysql_close(SQL);
}




void MySQL::Insert(string request)
{
//char *Req=new char[request.Length()+1];
//wcstombs(Req,request.c_str(),request.Length()+1);
mysql_query(SQL,request.c_str());
}

void MySQL::Delete(string request)
{
char *Req=new char[request.Length())
}
